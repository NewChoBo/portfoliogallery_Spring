# 사용할 베이스 이미지를 지정합니다. Java 17을 기반으로 하는 OpenJDK 이미지를 사용합니다.
FROM openjdk:17-alpine

# 컨테이너 내부에서 애플리케이션 파일들을 저장할 작업 디렉토리를 설정합니다.
WORKDIR /app

# 빌드된 JAR 파일을 현재 디렉토리(.)에서 컨테이너의 작업 디렉토리(/app)로 복사합니다.
# 빌드 과정에서 생성된 JAR 파일의 이름이 명확하지 않은 경우, 빌드 스크립트에서 JAR 파일 이름을 지정하거나
# Dockerfile을 수정하여 정확한 파일 이름을 사용해야 합니다.
COPY build/libs/*.jar app.jar

# 컨테이너가 시작될 때 실행될 명령어를 지정합니다. 애플리케이션 JAR 파일을 실행합니다.
ENTRYPOINT ["java", "-jar", "/app/app.jar"]

# 애플리케이션에 필요한 추가 환경 변수나 설정이 있다면, 여기에 ENV 명령어를 사용하여 추가할 수 있습니다.
